prototype(TYPO3.Neos:Content) {
	// Container Variable
	container= ${q(node).parent().parent().is(this.baseConfiguration.Container.Filter) ? this.baseConfiguration.Container.General : false}
	@context.container = ${this.container}
}
